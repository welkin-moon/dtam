{
  "name": "dtam",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check worker/src/index.js && node --test",
    "worker:dry-run": "wrangler deploy --config worker/wrangler.jsonc --dry-run",
    "worker:deploy": "wrangler deploy --config worker/wrangler.jsonc --keep-vars"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
