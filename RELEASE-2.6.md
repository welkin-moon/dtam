# Among Us · 东滩版 v2.6

## 核心修复

- 隔离幽灵通信：存活玩家不再显示死亡玩家的文字消息，也不订阅死亡玩家的语音；死亡玩家在游戏阶段使用仅幽灵可见的频道。
- 恢复主要交互链路：任务、紧急会议、通风管、信息设备、破坏、跳过投票和返回大厅重新绑定事件；返回大厅仅允许房主操作。
- 修复击杀后误触报告：击杀按钮位置保持稳定，并加入 900 ms 报告保护窗与统一操作节流。
- 客户端地图渲染压缩为 100×100、视野扩大到 26 格；与线上 Worker 的 150×150 坐标协议保持兼容，任务点、通风管、门禁、监控、Admin 和出生点均经过连通性验证。
- 恢复 6 位房间号，继续兼容旧 2 位房间；昵称持久化，刷新或重连时不再因随机昵称导致会话丢失。
- 头像改为全局持久化，并统一压缩为线上 Worker 可接受的 WebP 数据；旧头像会在兼容范围内迁移。
- 修复响应头错误禁用麦克风的问题；补充 HTTPS、权限、设备占用、无设备和浏览器不支持等明确提示。
- 修复隐藏 `<audio>` 导致部分移动端无法播放的问题；远端音频保留在渲染树中，并保留手动解锁入口。
- 收紧状态显示：普通游戏阶段不再通过玩家列表直接泄露死亡状态；幽灵聊天优先按稳定玩家 ID 判定，重复昵称不会造成频道串漏。
- 修正规则与场景提示：守护天使计入特殊船员总数，紧急会议台按房间配置次数正确高亮。

## 自动化验证

- JavaScript 语法与生产包哈希检查。
- 关键按钮、房主权限、幽灵通信、语音权限、头像、房间号和缓存版本静态断言。
- 100×100 客户端地图与 150×150 Worker 协议地图的坐标往返、连通性、任务点、通风管及门禁可达性检查。
